{
  "name": "multi-claude-session-prototype",
  "version": "0.0.1",
  "description": "Prototype for managing multiple Claude sessions in HumanCode IDE",
  "main": "out/extension.js",
  "engines": {
    "vscode": "^1.94.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.30.0"
  },
  "devDependencies": {
    "@types/vscode": "^1.94.0",
    "@types/node": "^18.x",
    "typescript": "^5.3.0"
  },
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  }
}
