# 执行摘要  

三份文档有较大重叠：第一、第二份主要描述平台级 AI 扩展互通的总体目标和架构，思路相似；第三份则是一个文档规划纲要，与前两份的设计内容重叠较少。总体来看，前两者内容重复较多，需要合并精简，后者可作为参考大纲。建议**合并必要内容**形成一份逻辑清晰、无冗余的文档，并通过优先级排序推进开发。  

## 原文档评估  

**1) “开发目标与边界” 文档**  
- *范围定义*：明确了此次迭代聚焦“平台级多 AI 扩展互通”，不涉 UI 自动化/OCR，也限定了先做本地再扩展远端/网页，还指出采用提议 API 先试行。目标清晰，但与第三份中的“PoC 路线”等内容有重复。  
- *冗余与缺漏*：与第二份提到的“实现文件结构”有部分交叉；缺少对具体技术挑战、兼容性问题（如焦点管理或 Web 安全）的深入说明。没有讨论测试或验收指标。  
- *风险*：若只依赖主观描述，可能导致边界定义不足（例如是否支持离线等）。应补充代码层面的具体限制（如扩展主机运行时、权限隔离等）【1†L140-L148】【11†L228-L232】。  

**2) “源码落点与改动地图” 文档**  
- *内容评估*：详细列出了文件路径和其职责列，但风格偏于列表化，没有说明动机。例如列出的各文件夹位置与 Code-OSS 分层是否吻合未讨论。  
- *冗余与缺漏*：与第一份的架构描述部分重合（都会谈到哪些模块需要改动）。但该文档重点在“放哪里”，缺少对改动内容本身的说明。未提及需要新增哪些 RPC 形状（MainContext/ExtHostContext），也未标明依赖关系是否合适。  
- *风险*：容易遗漏实际实现细节（例如应该在 `workbench/services` 下还是 `platform` 层）。应明确依据源码架构【10†L212-L220】放置模块，并补充各文件与现有模块的耦合关系。  

**3) “AI 扩展互通开发文档计划” 文档**  
- *内容评估*：本质上列出了需要编写的文档清单（目标、落点、协议、状态机、权限、适配、测试、PoC）。结构清晰，但不是真正的设计文档，而是文档清单模板。  
- *冗余与缺漏*：与前两份文档大部分内容重复（例如第一份的目标部分、第二份的文件分布，都会在这个清单中以提纲形式出现）。它本身不包含设计信息，仅是规划，缺少任何设计决策。  
- *风险*：单独看，这份文档无法支撑开发，只用于写作指导。依赖它可能导致“写文件式开发”。需要合并为实际内容，并根据前两份填充细节。  

## 合并后文档结构（目录）  

1. **执行摘要与范围说明**：概述整体目标、当前版本的实现边界、项目成功标准。  
2. **架构总览**：介绍新增“AI 互通总线”和“会话经纪人”两大核心组件的角色，与 VS Code 分层架构的关系。【10†L212-L220】  
3. **关键模块设计**：细化各模块职责，包括：  
   - **AI Interop 总线**：端点注册、路由、流式调用架构等；  
   - **AI 会话经纪**：会话模型、状态机、恢复机制；  
   - **权限/能力层**：扩展权限声明与用户授权流程；  
   - **适配层**：Command/Chat/Tool/MCP/Webview/CLI 适配方案；  
   - **观察与审计**：日志记录与 UI 面板设计。  
4. **源码变更落点**：列出具体文件路径及内容说明。示例如下：  

   | 文件路径                                              | 目的                                               | 内容概要                                     | 依赖/调用                                  |
   |:---------------------------------------------------|:-------------------------------------------------|:-----------------------------------------|:---------------------------------------|
   | `src/vs/workbench/services/aiInterop/common/aiInteropService.ts`   | 新建 Workbench 层服务接口（DSL层）【10†L212-L220】  | 定义 `IAIInteropBusService`, `IAISessionBrokerService` 等接口。 | 被 `mainThreadAiInterop`、`aiInteropService.ts` 实现。 |
   | `src/vs/workbench/services/aiInterop/browser/aiInteropService.ts`  | 实现工作台服务逻辑（UI 进程）                        | 主线程侧 AI 总线、会话经纪实现；注入 `IDialogService` 等。 | 依赖 `IExtensionService`, `IStorageService` 等。    |
   | `src/vs/workbench/api/browser/mainThreadAiInterop.ts`              | MainThread API 类，RPC 客户端【5†L8-L12】【7†L2068-L2070】 | 使用 `@extHostNamedCustomer(MainContext.MainThreadAiInterop)`；实现接口 `MainThreadAiInteropShape`，处理来自 ExtHost 的调用／进度。 | 调用 `rpcProtocol.set(MainContext.MainThreadAiInterop, …)` 注册；注入上述服务。 |
   | `src/vs/workbench/api/common/extHostAiInterop.ts`                  | ExtHost API 类，提供给扩展的入口                      | 实现 `ExtHostAiInterop`，负责 `vscode.proposed.aiInterop` 命令和方法；通过 `ExtHostContext.ExtHostAiInterop` 与主线程通信。 | 在 `createApiFactoryAndRegisterActors` 里 `rpcProtocol.set(ExtHostContext.ExtHostAiInterop, …)`。 |
   | `src/vs/workbench/api/common/extHost.protocol.ts`                  | 定义 RPC Shape 和 DTO                                | 增加 `MainThreadAiInteropShape`, `ExtHostAiInteropShape`；定义注册端点、调用、chunk、结果、取消等 DTO。 | 与主线程、扩展的对应类相匹配。          |
   | `src/vs/workbench/contrib/aiInterop/browser/aiInterop.contribution.ts` | 注册 UI 视图、命令和配置                            | 定义侧边栏面板、命令（如“授权提示”）等；将服务暴露给用户界面。 | 使用 `IViewDescriptorService` 注册面板；依赖 `aiInteropService`。 |
   | `src/vs/workbench/contrib/aiInterop/browser/aiInteropAuditView.ts`  | 审计日志面板                                        | 实现历史调用/会话浏览；支持过滤和导出。             | 从 `IAISessionBrokerService` 读取事件；显示给用户。      |
   | `src/vs/workbench/contrib/aiInterop/browser/aiInteropPermissionsView.ts` | 权限管理面板                                        | 显示当前扩展所获权限；列表化授权或拒绝记录。        | 结合 `IStorageService` 存储用户选择；调用 `IDialogService` 弹窗。 |

   以上文件路径均基于 Code-OSS 的层次结构：工作台层(`workbench/api`、`workbench/services`、`workbench/contrib`)。每个新文件应遵循已有模式（例如与 `MainThreadChatSessions`【7†L2068-L2070】 或 `MainThreadLanguageModelTools`【5†L8-L12】 等类似）。  

5. **公共接口与协议**：列出拟新增的 TypeScript 接口草案，示例：  
   ```ts
   export namespace aiInterop {
     export interface EndpointDescriptor { id: string; displayName: string; kind: 'agent'|'tool'|'ui'|'model'; /* … */ }
     export interface InvocationRequest { sessionId: string; prompt: string; /* … */ }
     export interface InvocationChunk { type: 'text'|'markdown'|'toolCall'; value: string; /* … */ }
     export interface InvocationResult { output?: string; errorMessage?: string; }
     export interface EndpointHandler {
       onInvoke(req: InvocationRequest, stream: InvocationResponseStream, token: CancellationToken): Thenable<InvocationResult>;
       /* 可选 onResume(...) */
     }
     export function registerEndpoint(descriptor: EndpointDescriptor, handler: EndpointHandler): Disposable;
     export function invokeEndpoint(endpointId: string, request: InvocationRequest): Disposable; 
   }
   ```
   以及对应的 `MainContext.MainThreadAiInterop` 和 `ExtHostContext.ExtHostAiInterop` 新枚举值，以及在 `rpcProtocol` 中应使用的消息类型和取消令牌机制（参考 VS Code 的取消实现【11†L228-L232】）。  

## 实施路线与优先级  

按照风险和价值排序，分阶段推进：  

| 阶段      | 时间估算(人周) | 主要任务                                | 验证实验与指标                                             |
|:---------|:----------|:------------------------------------|:--------------------------------------------------------|
| **PoC**  | 2–4      | 实现 AI Bus 最简闭环：本地端点注册、单次流式调用、取消。 | **实验1：跨-host 1** – 在同一 Node Host 上注册两个端点，测试流式聊天输出；<br>**指标**：p95 响应延迟 <50ms，chunk 通信正确率 100%；无内存泄露。 |
| **MVP**  | 6–8      | 增加会话经纪（Session Broker）、权限校验；实现跨-host 路由；实现审计/日志。 | **实验2：流式+取消+背压** – 发送 200msg/s 大量流（模拟 LLM Token 流），期间尝试取消。<br>**指标**：主线程 CPU 占用<5%；取消命令生效延时 <200ms；backpressure 无阻塞。 |
| **Phase 2** | 8–12     | 集成工具调用与 MCP 支持；开发 CLI/终端 适配；多场景兼容测试。 | **实验3：端点认证** – 让扩展 A 伪造 B 的 endpoint 调用；测试拒绝策略。<br>**实验4：权限 UX** – 用户选择框验证：记录“授权成功率”。要求无误操作提示，>95% 用户通过率。<br>**实验5：工具仲裁** – 同时触发多工具调用，验证只调用最佳匹配，错误处理正确。 |
| **Phase 3** | 6–8      | 打磨稳定性和兼容性：异步恢复、多设备同步；发布稳定 API。 | 长时间运行（如 24h 收发百万次消息）无崩溃；与新版 VS Code 兼容测试通过（未出现扩展隔离问题）。 |

*时间和 SLO 未指定，仅供预估。*

## 推荐预备文档

在编码前，建议整理以下文档（按优先级）并做好模板：  

- **执行摘要与目标范围**：概述本期工作目标与不做事项，明确成功标准（例：支持哪些场景，不包含 UI hack）。  
- **核心架构设计文档**：详细描述 AI Interop 总线、会话经纪、权限模型等，附架构图。  
- **接口协议文档（RPC/DTO）**：列出所有 RPC 形状和数据结构的规范，而不是代码注释。  
- **会话状态机文档**：绘制 Session/Invocation 的状态图，说明生命周期与恢复机制。  
- **权限安全文档**：定义扩展权限模型、用户授权流程和审计需求。  
- **Adapter 方案文档**：对 Command/Chat/Tool/MCP/Webview/CLI 接入的兼容策略逐一说明。  
- **测试验收文档**：四层测试计划和验收指标（见上表），确保可量化评估。  
- **开发计划手册**：分阶段任务清单，给团队和自动化工具使用。  

**模板示例**（简要列项）：  

- *执行摘要.doc*：工作目的、范围、不做项、一句话总结。  
- *架构设计.md*：模块列表+图表；每部分说明（参照 [10†L212-L220]）。  
- *协议规范.md*：枚举所有接口签名和数据字段。  
- *状态机.md*：文字+状态流图，说明转移条件。  
- *权限模型.md*：角色/扩展 许可矩阵、流程示意图。  
- *适配器指南.md*：按类型列出输入/输出与授权需求。  
- *测试方案.md*：测试层次、用例清单、性能基准与模拟策略。  
- *实施手册.md*：各阶段里程碑+任务清单+验收清单。  

## 图表示例  

```mermaid
flowchart TB
  subgraph 总线[AI Interop 总线服务]
    Bus[Message Bus Service]
    Broker[Session Broker Service]
    Policy[Policy/Permission Service]
    Audit[Audit/Log Service]
  end
  subgraph 主线程[Workbench Main Thread]
    Bus
    Broker
    Policy
    Audit
  end
  subgraph ExtHostA[Ext Host: Controller AI]
    A(控制扩展)
    AEP[Endpoint API]
  end
  subgraph ExtHostB[Ext Host: Worker AI]
    B(被调扩展)
    BEP[Endpoint API]
  end
  subgraph ExtHostC[Ext Host: CLI/Tool]
    C(CLI Agent)
    CEP[Endpoint API]
  end
  subgraph 外部[外部系统]
    MCP[MCP 服务]
    ToolProvider[工具提供者]
  end

  A -- 注册Endpoint --> Bus
  B -- 注册Endpoint --> Bus
  C -- 注册Endpoint --> Bus
  A -- invoke(request) --> Bus
  Bus --> Broker
  Broker --> Policy
  Policy --> Broker
  Broker --> Bus
  Bus -- route --> B
  B -->|流程输出| Bus
  Bus -->|relay| A
  B -- toolCall --> ToolProvider
  ToolProvider -->|结果| Bus
  Bus --> A
  C -- invokeCLI --> Bus
  Bus --> Broker
  Broker --> C
```  

```mermaid
sequenceDiagram
    participant 主控AI as A
    participant 总线服务 as Bus
    participant 扩展主线程 as Main
    participant 目标AI as B
    A->>Bus: invokeEndpoint(请求)
    Bus->>Policy: 校验权限
    alt 允许
        Bus->>Main: $invokeEndpoint(request)
        Main->>ExtHostB: 转发调用
        ExtHostB-->ExtHostB: 处理请求 (异步)
        loop 流式返回 chunks
            ExtHostB->>Main: $acceptInvocationChunk(chunk)
            Main->>Bus: onChunk(chunk)
            Bus->>A: deliverChunk(chunk)
        end
        ExtHostB->>Main: $completeInvocation(result)
        Main->>Bus: onComplete(result)
        Bus->>A: deliverResult(result)
    else 拒绝
        Bus->>A: 错误(Unauthorized)
    end
```

```mermaid
gantt
  title 交付路线图
  dateFormat  YYYY-MM-DD
  section PoC
    总线基础功能   :done,    2026-04-01, 2026-04-14
  section MVP
    会话与权限   :active,  2026-04-15, 2026-05-15
  section Phase2
    工具与CLI集成 :        2026-05-16, 2026-06-30
  section Phase3
    稳定性与治理   :        2026-07-01, 2026-08-31
```

**注**：以上示例中时间为假设，仅为展示用。各栏说明按项目具体情况定。

